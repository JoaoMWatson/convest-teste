CREATE TABLE IF NOT EXISTS price(
    id integer PRIMARY KEY,
    id_stock NOT NULL
    value text NOT NULL
    date text NOT NULL UNIQUE,
    FOREIGN KEY (id_stock) REFERENCES stock (id)
);

CREATE TABLE IT NOT EXISTS stock(
    id integer PRIMARY KEY,
    ticket text NOT NULL UNIQUE,
    name text UNIQUE,
    active integer,
);
